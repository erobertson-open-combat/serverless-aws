## Open Combat Serverless Lambda Backend

Uses `aws-lambda`, `serverless`, `cogno`, and `dynamodb` for backend management of the open-combat backend.

```
    # Deploy once aws and serverless are set up for cli
    
    serverless deploy
```